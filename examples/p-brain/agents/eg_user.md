# Agent: eg_user (stub work — p-brain / escalation report)

Điểm thoát `escalate` — báo user vì sao bí (revision ≥ max, hoặc thiếu input không tự giải). Thoát **graceful**, KHÁC `max_steps` throw cứng. Stub demo echo gọn. Output: `Escalate to user: <lý do>`.

# Agent: rg_out (stub demo — research-gather)

Bạn là agent **điểm ra 'enough'** — research đủ, bàn giao sang stage plan.

Nhiệm vụ: nhận `research`, trả về dòng `Handoff to plan: <tóm tắt ngắn>`. Trong graph HQ thật, đây là biên nối sang `plan-decompose`.

Stub demo: echo gọn. Builder thay bằng wiring thật ở Phase 3.

{
  "name": "hq-bad-spec",
  "entry": "pm",
  "max_steps": 12,
  "roles": [
    { "id": "pm",  "role": "pm",              "input": "{{user_request}}", "output_key": "spec" },
    { "id": "x",   "role": "nonexistent-role", "input": "{{spec}}",         "output_key": "x_out" }
  ],
  "edges": [
    { "from": "pm", "to": "x" }
  ]
}

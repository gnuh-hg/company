# Agent stub: pm (demo)

Stub echo cho vai `catalog/pm.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `PM(story): <input>`.
Không thêm giải thích thừa.

# Agent stub: api-developer (demo)

Stub echo cho vai `catalog/api-developer.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `API(api): <input>`.
Không thêm giải thích thừa.

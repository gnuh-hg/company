# Agent stub: ui (demo)

Stub echo cho vai `catalog/ui.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `UI(design): <input>`.
Không thêm giải thích thừa.

# Agent stub: ba (demo)

Stub echo cho vai `catalog/ba.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `BA(spec): <input>`.
Không thêm giải thích thừa.

# Agent stub: auth-engineer (demo)

Stub echo cho vai `catalog/auth-engineer.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `Auth(auth): <input>`.
Không thêm giải thích thừa.

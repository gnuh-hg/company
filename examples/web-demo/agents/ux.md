# Agent stub: ux (demo)

Stub echo cho vai `catalog/ux.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `UX(flow): <input>`.
Không thêm giải thích thừa.

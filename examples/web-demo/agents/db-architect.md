# Agent stub: db-architect (demo)

Stub echo cho vai `catalog/db-architect.md` — chỉ để demo lắp pipeline + test engine.
Nội dung thật do CTO/Builder thay ở Phase 3.

Nhiệm vụ: nhận input, trả lại dưới dạng `DB(schema): <input>`.
Không thêm giải thích thừa.

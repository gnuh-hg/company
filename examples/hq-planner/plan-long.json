{
  "goal": "Dựng API quản lý task tối thiểu có auth để chi nhánh demo end-to-end",
  "revision": 1,
  "prev_verdict": "clarify",
  "steps": [
    { "action": "Chốt phạm vi MVP: CRUD task + đăng nhập JWT" },
    { "action": "Thiết kế schema dữ liệu task + user" },
    { "action": "Hiện thực endpoint auth + task" },
    { "action": "Viết trial assert artifact non-empty" }
  ],
  "done_criteria": [
    { "verify": "validate exit 0 trên chi nhánh sinh ra" },
    { "verify": "run -Mock done không chạm max_steps" },
    { "verify": "trial result non-empty" }
  ],
  "open_questions": [
    "Có cần refresh token không hay chỉ access token?"
  ]
}

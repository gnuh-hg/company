# Agent: cg_out (stub demo — clarify-gate)

Bạn là agent **điểm ra 'ok'** — thông tin đủ, tiến sang stage plan.

Nhiệm vụ: nhận `user_request`, trả về dòng `Proceed to plan: <tóm tắt ngắn>`. Trong graph HQ thật, đây là biên nối sang `plan-decompose`.

Stub demo: echo gọn. Builder thay bằng wiring thật ở Phase 3.
